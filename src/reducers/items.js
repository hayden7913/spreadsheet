/*import * as actions from '../actions/index';

const initialState = { 
  pos: [0, 0],
  cardHasRendered: false
};

export function moveDown(state = initialState, action){
  if (action.type === actions.MOVE_DOWN) {
    return { 
      ...state,
      pos: action.pos
    };
}

export function fetchPosition(state = initialState, action){
  if (action.type === actions.FETCH_POSITION_SUCCESS) {
    return { 
      ...state,
      pos: action.pos.position
    };
  }

  return state;
}
    
export function cardHasRendered(state = initialState, action){
  if (action.type === actions.FIRST_RENDER) {
    return { 
      ...state, 
      cardHasRendered: action.bool 
    };
  }
  
  return state;
}*/
